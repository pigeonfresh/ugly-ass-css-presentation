import AbstractBlock from "../AbstractBlock";

export default class FlexboxAligning extends AbstractBlock {
  static displayName:string = 'flexbox-aligning';

  constructor(el:HTMLElement) {
    super(el);
  }

  public dispose() {
    super.dispose();
  }
}
